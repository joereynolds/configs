# Setup

All of this is required BEFORE doing anything else.
Some of Vim's plugins require the tools here
to compile. Without the setup step, you're
left to do it on your own. Good luck.

## Other

- Remove the shit version of vim and link the new one
    sudo rm -rf /usr/local/bin/vim
    ln -s /usr/bin/vim.gnome /usr/local/bin/vim

- Create symlinks
    - Vim
        ln -s ~/programs/configs/vim/.vimrc ~/.vimrc
    - Bash
        ln -s ~/programs/configs/bash/.bashrc ~/.bashrc
    - Conky
        ln -s ~/programs/configs/conky/.conky.conf ~/.conky.conf
    - Openbox themes
        ln -s ~/programs/configs/openbox/.themes ~/.themes

- Get a background
    cd ~/Pictures
    feh --bg-fill yourPicture.jpg


# Optional

- Change openboxes terminal hotkeys
    - ctrl-z for previous tab
    - ctrl-x for next tab


# Vim

- get pathogen
    mkdir -p ~/.vim/autoload ~/.vim/bundle && \
    curl -LSso ~/.vim/autoload/pathogen.vim https://tpo.pe/pathogen.vim
