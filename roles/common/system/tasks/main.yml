- name: "Create required directories"
  file:
    path: "{{ item }}"
    state: directory
  with_items:
      - ~/.newsbeuter
      - ~/code/me

- name: "Symlink configuration files"
  file:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    state: link
  with_items:
    - { src: '~/code/me/configs/dotfiles/nvim/init.vim',   dest: '~/.config/nvim/init.vim' }
    - { src: '~/code/me/configs/dotfiles/bash/.bashrc',    dest: '~/.bashrc' }
    - { src: '~/code/me/configs/dotfiles/mutt/.muttrc',    dest: '~/.muttrc' }
    - { src: '~/code/me/configs/dotfiles/newsbeuter/urls', dest: '~/.newsbeuter/urls' }
    - { src: '~/code/me/configs/dotfiles/ctags/.ctags',    dest: '~/.ctags' }
